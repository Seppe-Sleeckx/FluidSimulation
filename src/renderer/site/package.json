{
  "name": "site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:full": "npm run build:wasm && vite",
    "build": "npm run build:wasm && tsc && vite build",
    "build:wasm": "cmake --build ../../../build/wasm --target fluid_wasm",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@webgpu/types": "^0.1.68",
    "gl-matrix": "^3.4.4"
  }
}
